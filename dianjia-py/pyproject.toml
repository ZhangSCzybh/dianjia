[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "dianjia-memory"
version = "0.1.0"
description = "A Markdown-first personal memory system"
requires-python = ">=3.11"

[project.scripts]
dianjia = "app.main:main"


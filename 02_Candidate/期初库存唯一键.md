# 期初库存唯一键

memory_id：`sql-candidate-opening-inventory-unique-key-001`

分类：SQL / 字段血缘

标签：期初库存、唯一键、brz_type、数据质量

评分：15

状态：candidate

## 候选原因

已知 `brz_type=-1` 和 `brz_type=-2` 的来源含义，但尚未确认用于“重复记录”判断的完整业务唯一键，以及月初切换和补数策略。

## 核心内容

期初库存存在重复不必然代表数据错误，必须先按真实业务唯一键、来源类型和生成批次判断。未明确唯一键前不能直接删除或合并记录。

## 升级条件

取得期初库存表结构、生成任务逻辑和至少一组重复样本后，更新 [[期初库存与 brz_type]]。

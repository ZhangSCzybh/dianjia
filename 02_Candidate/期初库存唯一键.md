# 期初库存唯一键

memory_id：`sql-candidate-opening-inventory-unique-key-001`

分类：SQL / 字段血缘

标签：期初库存、唯一键、biz_type、数据质量

评分：15

状态：candidate

## 候选原因

已取得 `flowstock.dm_wms_stock_period_12562` 的表结构。其 `SharedReplacingMergeTree` 替换去重键为 `(bill_date, brand_id, storage_id, sku_id, source_id, key_id)`，版本字段为 `sync_last_modify_time`；但尚未确认该物理键是否等同业务唯一键，以及是否应额外包含 `biz_type`。

## 核心内容

期初库存存在重复不必然代表数据错误：普通查询可能看到尚未合并的多版本物理记录，而 `FINAL` 会按替换去重键保留最新版本。`biz_type` 不在 `ORDER BY` 中，是否应作为业务唯一键的一部分仍待确认；未明确前不能直接删除或合并记录。

## 升级条件

取得生成任务逻辑和至少一组重复样本后，确认 `biz_type`、`source_id`、`key_id` 的业务关系，并更新 [[期初库存与 biz_type]]。
